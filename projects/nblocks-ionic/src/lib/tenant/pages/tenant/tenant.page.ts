import { Component, OnInit } from '@angular/core';

@Component({
  selector: 'nblocks-tenant-page',
  templateUrl: './tenant.page.html',
  styleUrls: ['./tenant.page.scss'],
})
export class TenantPage implements OnInit {

  constructor(
    ) { }

  ngOnInit(): void {
  }
}
